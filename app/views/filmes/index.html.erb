<p id="notice"><%= notice %></p>


<%= image_tag 'lista.png', class: 'menor' %>


<table>
  <thead>
    <tr>
      <th>Titulo</th>
      <th>Genero</th>
      <th>Ator</th>
      <th>Preco</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @filmes.each do |filme| %>
      <tr>
        <td><%= filme.titulo %></td>
        <td><%= filme.genero %></td>
        <td><%= filme.ator %></td>
        <td><%= filme.preco %></td>
        <td><%= link_to 'Show', filme %></td>
        <td><%= link_to 'Edit', edit_filme_path(filme) %></td>
        <td><%= link_to 'Destroy', filme, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate @filmes, previous_label: "Anterior", next_label: "Próxima" %>
<br/>

<br>
<br>

<%= link_to 'Novo Filme', new_filme_path%>
